// Function to collect user information
function collectUserInfo() {
    // Storing user information in variables
    const fullName = "Joshua Toquero";  // Replace with prompt if needed
    const age = 20;                      // Replace with prompt if needed
    const favoriteNumber = 25;           // Replace with prompt if needed
    const favoriteColor = "black";       // Replace with prompt if needed

    // Logging the collected information to the console
    console.log("User Information:");
    console.log("Full Name: " + fullName);
    console.log("Age: " + age);
    console.log("Favorite Number: " + favoriteNumber);
    console.log("Favorite Color: " + favoriteColor);
}

// Call the function to execute it
collectUserInfo();
