// Create a userProfile object to store user information
const userProfile = {
    fullName: "Joshua M. Toquero",
    age: 20,
    favoriteNumber: 25,
    favoriteColors: ["black", "white", "red"]
};

// Log the entire object to the console
console.log(userProfile);